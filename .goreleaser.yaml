version: 2

project_name: agentroute

before:
  hooks:
    - go mod tidy

builds:
  - id: agentroute
    main: ./cmd/agentroute
    binary: agentroute
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/Radixen-Dev/AgentRoute/internal/version.Version={{.Version}}
      - -X github.com/Radixen-Dev/AgentRoute/internal/version.Commit={{.Commit}}
      - -X github.com/Radixen-Dev/AgentRoute/internal/version.Date={{.Date}}

archives:
  - id: agentroute-archive
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

signs:
  - cmd: cosign
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--output-certificate=${certificate}"
      - "--output-signature=${signature}"
      - "${artifact}"
      - --yes
    artifacts: checksum

homebrew_casks:
  - name: agentroute
    repository:
      owner: Radixen-Dev
      name: homebrew-agentroute
      # The default GITHUB_TOKEN is scoped to this repo only and can't
      # write to a sibling repo — use a PAT with contents:write on
      # homebrew-agentroute instead.
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    directory: Casks
    homepage: "https://github.com/Radixen-Dev/AgentRoute"
    description: "Route Claude Code (and friends) through OpenRouter with a modern, animated TUI."
    license: "GPL-3.0-only"

scoops:
  - name: agentroute
    repository:
      owner: Radixen-Dev
      name: scoop-agentroute
      # Same cross-repo write problem as the homebrew cask above.
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/Radixen-Dev/AgentRoute"
    description: "Route Claude Code (and friends) through OpenRouter with a modern, animated TUI."
    license: "GPL-3.0-only"

release:
  github:
    owner: Radixen-Dev
    name: AgentRoute

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
